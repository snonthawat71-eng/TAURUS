-- ============================================================
-- เพิ่มคอลัมน์เก็บ "สี avatar" ของผู้เดินทาง
-- วางใน Supabase > SQL Editor แล้วกด Run ครั้งเดียว
-- ปลอดภัย: ไม่ลบ/แก้ข้อมูลเดิม (คอลัมน์เป็น NULL ได้ ของเดิมจะใช้สีตามลำดับ)
-- ============================================================
alter table travelers add column if not exists avatar_color text;
