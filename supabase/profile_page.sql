-- ============================================================
-- TAURUS — หน้า Profile: เป้าหมายการเดินทางรายปี
-- เก็บเป็น jsonb map ปี→จำนวนช่องเป้าหมาย เช่น {"2026": 8}
-- รันซ้ำได้ปลอดภัย
-- ============================================================
alter table profiles add column if not exists year_goal jsonb;
