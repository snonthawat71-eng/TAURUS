-- stop_branch.sql — "ร้านนี้ วันนี้ไปสาขาไหน"
-- รันใน Supabase SQL Editor ครั้งเดียว · รันซ้ำได้ปลอดภัย
--
-- เดิมสาขาที่เลือกถูกเก็บไว้ที่ "ร้าน" (places.plan_branch) = ค่าเดียวต่อทริป
-- ทำให้ร้านเชนไปได้แค่สาขาเดียว ต่อจากนี้เก็บที่ "จุดแวะของแต่ละวัน" แทน
-- → ร้านเดียวใส่ได้หลายวัน คนละสาขา (เช่น Ichiran ชิบูย่าวันที่ 2, ชินจูกุวันที่ 5)
--
-- branch_idx = ลำดับสาขาใน places.branches (null = ที่ตั้งหลักของร้าน)
-- ไม่แตะข้อมูลเดิมเลย: จุดแวะเก่าเป็น null ซึ่งอ่านค่าจาก places.plan_branch
-- ต่อไปเหมือนเดิม (โค้ดรองรับทั้งสองแบบ)

alter table public.itinerary_stops add column if not exists branch_idx int;
